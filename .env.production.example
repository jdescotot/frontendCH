# Si frontend y API comparten dominio, déjalo vacío y las llamadas serán /api/v1/...
VITE_API_BASE_URL=

# Si la API termina en otro dominio, define la URL pública completa, sin barra final.
# Ejemplo:
# VITE_API_BASE_URL=https://api.controlhorario.es
