# En desarrollo, Vite reenvía /api y /health al backend.
VITE_DEV_API_PROXY_TARGET=http://localhost:8080

# Déjalo vacío para usar el mismo dominio: /api/v1/...
# Solo usa una URL completa si el backend permite CORS y cookies entre dominios.
VITE_API_BASE_URL=
